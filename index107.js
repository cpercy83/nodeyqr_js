function biggerNumber () {
    firstNumber.typeOf(number);
    secondNumber.tpeof(number);
    while (firstNumber>secondNumber) {
        console.log(`The number ${firstNumber} is greater than the number ${secondNumber}`);
        }
    while (secondNumber>firstNumber) {
        console.log(`The number ${secondNumber} is greater than the number ${firstNumber}`);
        }
    while (firstNumber=secondNumber) {
        console.log(`The numbers ${firstNumber} and ${secondNumber} are equal`);
        }
    }

    let firstNumber = 3
    let secondNumber = 5

    biggerNumber(firstNumber,secondNumber);